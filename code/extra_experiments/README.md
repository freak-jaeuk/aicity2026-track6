# Extra experiments (NOT used to produce the paper results)

These scripts were part of the working repository but are not part of the
reported L0-L4 pipeline:

- export_onnx.py  - ONNX export
- stress_eval.py  - internal stress/latency probing
- sweep_resolution.py - resolution-sweep scaffold (future work, never run; paper section 7)
